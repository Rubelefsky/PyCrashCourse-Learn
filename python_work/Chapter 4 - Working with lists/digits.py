digits = [1, 2, 3, 4, 5, 6, 7, 8, 9, 0] # Makes a list 0-9
min(digits) # In Console prints Minimum number which is 0
max(digits) # Prints largest number which is 9
sum(digits) # Adds all of the digits which is 45