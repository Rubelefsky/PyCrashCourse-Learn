squares = [] # Empty List
for value in range(1, 11): # Loop through 1-10
    square = value ** 2 # Each value gets raised to the second power
    squares.append(square) # Each new value gets added to the list

print(squares) # prints the list which has the new values added